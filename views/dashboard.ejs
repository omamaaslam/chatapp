<!-- <style>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
      Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue",
      sans-serif;
  }

  body {
    background: #2f2b33;
  }

  nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 20px;
    height: 70px;
    padding: 21px;
    border-bottom: 1px solid #3ac999;
    background-color: #3ac999;


    position: sticky;
    top: 0;
    left: 0;
    width: 100%;
    margin-bottom: 20px;
  }

  .nav-brand h1 {
    font-size: 20px;
    color: white;
    text-transform: uppercase;
  }

  nav ul {
    list-style: none;
    display: inline-block;
  }

  nav ul li {
    border: 1px solid red;
    background-color: red;
    padding: 10px;
    border-radius: 10px;
  }

  ul li a {
    text-decoration: none;
    color: white;
    font-weight: 600;
    font-size: 18px;
  }

  nav ul li:hover {
    opacity: 0.8;
    transition: ease-in-out 0.5s;
  }
</style>

<nav>
  <div class="nav-brand">
    <h1><%= user.name %></h1></div>
  <ul>
    <li><a href="/logout">Log Out</a></li>
  </ul>
</nav> -->

<%- include('layout/header.ejs') %>

<div id="content" class="p-4 p-md-5 pt-5">
  <div class="row mb-3">
    <h2 class="mb-4"><%= user.name %></h2>
  </div>
  <div class="row mb-3">
    <div class="col-md-3">
      <ul class="list-group">
        <% if(users.length > 0) { %> <% for(let i = 0; i < users.length; i++) {
        %>
        <li class="list-group-item list-group-item-dark cursor-pointer">
          <img
            src="<%= 'http://localhost:5000/' + users[i]['image'] %>"
            alt="img"
            width="50px"
            height="50px"
          />
          <%= users[i].name %>
        </li>
        <% } %> <% } %>
      </ul>
    </div>
    <div class="col-md-9">
      <span class="text-primary h6">Click to Start Chat</span>
      <div class="chat_section">
        <div class="chat_container"></div>
        <div class="chat_form">
          <div class="input-group mb-3">
            <input
              type="text"
              class="form-control"
              placeholder="Send Message"
            />
            <button class="btn btn-primary" type="button">Send</button>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<%- include('layout/footer.ejs') %>
